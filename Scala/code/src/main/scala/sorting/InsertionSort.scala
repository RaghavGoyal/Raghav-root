package sorting

/**
 * Insertion Sort compares the element with its preceding element and the smaller of the two is placed at beginning.
 */
object InsertionSort extends App {

  val list = List(10, 11, 13, 9, 5, 30, 14, 99, 55, 19, 29, 35, 80, 75)

//  val sortedList = insertionSort(list)
//  println(sortedList)

//  private def insertionSort(list: List[Int]): List[Int] = {
//    list match {
//      case Nil => Nil
//      case a::Nil => List(a)
//      case sorted::unsorted =>
//        if(sorted.tail < unsorted.head)
//    }
//  }

}
